import React from "camunda-modeler-plugin-helpers/react";

import {} from "../utils/metrics/all";

function useMetrics() {
    const [metrics, setMetrics] = React.useState([]);
    function removeMetricWithCategories(categories, metric) {}
}
